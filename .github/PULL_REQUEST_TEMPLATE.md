<!--
Thanks for contributing to middag-io/ui!
Please fill in the sections below. PRs target the `develop` branch.
-->

## Summary

<!-- What does this PR change, and why? Keep it focused on a single concern. -->

## Related issues

<!-- Link issues this PR addresses. Use "Closes #123" to auto-close on merge. -->

Closes #

## Type of change

<!-- Check all that apply. These mirror the Conventional Commit type used in your commit messages. -->

- [ ] `feat` — a new feature
- [ ] `fix` — a bug fix
- [ ] `refactor` — a code change that neither fixes a bug nor adds a feature
- [ ] `docs` — documentation only
- [ ] `test` — adding or correcting tests
- [ ] `chore` — tooling, build, or maintenance changes
- [ ] `perf` — a performance improvement
- [ ] `style` — formatting only (no behavior change)

## Breaking change

<!--
Does this PR change public API or the wire contract in a backward-incompatible
way? If so, add a `BREAKING CHANGE:` footer to the relevant commit and describe
the break below.

Note: on the `1.x` line a breaking change ships only in a MINOR release cut
deliberately by a maintainer (`Release-As:` footer) — never in a patch. The
footer is what surfaces the break in the CHANGELOG's breaking section; without
it the break would ship silently.
-->

- [ ] This PR introduces a breaking change (a `BREAKING CHANGE:` footer is present on the relevant commit)

## Checklist

- [ ] Targets the `develop` branch
- [ ] `composer check` passes locally
- [ ] `composer test` passes locally
- [ ] Commits follow Conventional Commits
- [ ] No `Co-Authored-By` trailers in any commit
- [ ] Docs / `CHANGELOG.md` updated when relevant
- [ ] New behavior is covered by tests
